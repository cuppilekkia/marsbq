<section id="menu">
  <h2>Menu</h2>
  <p>Vota il tuo favorito</p>

  <ul>
  {{#each menu}}
    <li data-id="{{id}}">
      <span class="menu-title">{{title}}</span>
      <span class="menu-count">{{count}}</span>
    </li>
  {{/each}}
  </ul>
  
</section>
